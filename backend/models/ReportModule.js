module.exports = (sequelize, DataTypes) => {
    const Report = sequelize.define("Repost", {
    // TODO добавить ID (user, weather forecast, result, defects, gallery, location, recommendation, )
    });
};